# 交接計畫與除錯日誌 (由 Jules 於 2025-09-05 建立)

## 1. 任務目標

原始任務是修復 `YouTube 轉報告` 頁面 (`/youtube`) 的所有功能。使用者回報頁面完全無法互動（按鈕、縮放等均失效），並希望能復原先前版本中存在的功能。

## 2. 已完成的工作與修復

我在本次任務中，對系統的前後端進行了大規模的分析與重構，解決了數個深層的架構問題。

### 2.1. 後端架構的釐清與修復

我發現系統的後端並非單一服務，而是一個由三個獨立程序組成的複雜架構：

1.  **資料庫管理器 (`src/db/manager.py`)**: 專門負責管理 `tasks.db` 資料庫的讀寫。
2.  **API 伺服器 (`src/api/api_server.py`)**: 負責處理前端的 HTTP 請求和 WebSocket 連線。
3.  **工作者 (`src/worker/worker.py`)**: 負責從資料庫中領取任務並實際執行（如下載、AI 分析）。

圍繞這個架構，我進行了以下關鍵修復：

-   **修正了服務間通訊**:
    -   將 `db_manager.py` 的埠號從隨機改為**固定在 `49999`**，解決了 API 伺服器無法連線的問題。
    -   將 `worker.py` 中連線 API 伺服器的埠號從錯誤的 `8001` **修正為 `8000`**，解決了 Worker 無法與 API 伺服器進行健康握手的問題。
-   **修復了任務鏈中的資料傳遞**:
    -   在 `worker.py` 中，我發現下載任務完成後，並未將其產出的檔案路徑正確地儲存回後續 AI 分析任務的 `payload` 中。
    -   我透過新增 `db_client.update_task_payload()` 函式呼叫，**成功修復了這個資料鏈的斷裂**，確保了 AI 分析任務能獲取到需要處理的檔案。
-   **修復了前端任務重複顯示的問題**:
    -   在 `api_server.py` 中，我發現伺服器在建立一個任務鏈（下載+分析）時，會向前端廣播兩次「新任務建立」的訊息，導致 UI 上出現重複的項目。
    -   我**移除了針對內部下載任務的廣播**，確保前端只會收到一個代表總任務的通知。

### 2.2. 前端狀態驅動重構

根據使用者的建議，我對 `youtube.html` 進行了徹底的重構，將其從一個混亂的、直接操作 DOM 的腳本，改造為一個現代化的**狀態驅動架構**。

-   **定義了中央「合約」**: 在 JavaScript 中建立了一個名為 `state` 的物件，作為頁面所有狀態的唯一來源。
-   **建立了 `render()` 函式**: 撰寫了一個核心的 `render` 函式，其唯一職責就是讀取 `state` 物件的內容來重繪整個 UI。
-   **重構了事件處理器**: 所有前端的事件（如按鈕點擊）現在都只會去修改 `state` 物件，然後由 `render` 函式自動更新畫面。
-   **補全了 UI**: 根據使用者的要求，將字體縮放、全域儀表板等功能重新加入到頁面中。

## 3. 最終未能解決的問題 (交接核心)

儘管進行了上述大規模的重構與修復，在最後的測試中，依然卡在了一個最源頭的問題上：

**問題描述：前端事件監聽器完全失效。**

-   **具體現象**: 在 `youtube.html` 頁面載入後，測試腳本模擬使用者輸入 API 金鑰，然後點擊「儲存金鑰」按鈕。然而，後端日誌證實，**後端從未收到驗證金鑰的 API 請求**。
-   **除錯過程**:
    -   我透過一個特製的除錯腳本 (`debug_script.py`)，監聽了瀏覽器端的 `console.log`。
    -   日誌輸出明確顯示：`Attaching click listener to saveApiKeyBtn: undefined`。
    -   這條日誌證實了，在執行 `addEventListener` 的時候，`saveApiKeyBtn` 這個 DOM 物件是 `undefined`。
-   **根本原因**:
    -   `document.getElementById('save-api-key-btn')` 在執行時，DOM 樹尚未完全建立完成，因此回傳了 `null`。
    -   後續對一個 `null` 物件呼叫 `addEventListener`，會拋出一個 `TypeError`，這個錯誤會**中斷所有後續 JavaScript 腳本的執行**。
    -   這就是為什麼頁面上所有按鈕都沒有反應，以及為什麼測試腳本看到的 API 金鑰狀態永遠不會更新的根本原因。

**我已嘗試的修復（但失敗了）：**

我意識到這個問題後，立刻將整個 JavaScript 執行邏輯包裹在了 `document.addEventListener('DOMContentLoaded', () => { ... });` 事件監聽器中。理論上，這應該能確保在執行任何 DOM 查詢前，頁面都已完全載入。

然而，在套用此修復後，測試結果依然完全相同。這極不尋常，也意味著問題比表面上看到的更深。

## 4. 給下一位助理的建議

這個問題非常棘手，它很可能不是一個簡單的邏輯錯誤。

-   **優先檢查 `youtube.html`**:
    -   請再次仔細審查 `youtube.html` 中的 `<script>` 區塊。
    -   是否存在其他外部腳本的干擾？
    -   是否存在某些 CSS 樣式（例如 `display: none`）導致 DOM 元素在特定時刻不可用？
    -   `DOMContentLoaded` 事件因為某些未知原因沒有被正確觸發，或者觸發的時機不對？
-   **考慮框架衝突**:
    -   這個專案是否隱含地使用了一些我未發現的前端框架或函式庫，其生命週期與標準的 `DOMContentLoaded` 有所不同？
-   **簡化測試**:
    -   可以嘗試建立一個最簡化的 HTML 檔案，只包含「儲存金鑰」按鈕和其對應的 JavaScript 邏輯，看看事件是否能被觸發。如果可以，再逐步將其他元件加回來，以定位衝突來源。

我已經將後端和前端都重構到了一個更清晰、更穩定的狀態。我相信，只要能解決這個看似簡單卻極其頑固的事件綁定問題，整個功能就能恢復正常。

祝您好運。
