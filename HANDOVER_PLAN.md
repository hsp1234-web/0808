# 交接報告：v2 E2E 測試的頑固超時問題

**日期**: 2025-09-04
**交接人**: Jules
**交接對象**: 下一位接手的開發助理

## 1. 總體目標與當前狀態

**目標**: 完成 v2 前端架構（從 SPA 到 MPA）的重構，並確保所有 E2E 測試 100% 通過。

**當前狀態**: **卡關**。儘管已對測試套件進行了大規模的修復，但在執行完整的 E2E 測試 (`npx playwright test --config=playwright.config.v2.js`) 時，仍然會發生**全局性的超時 (Timeout)**，導致測試無法完成。

## 2. 已完成的修復工作

在本次工作階段，我成功地、系統性地解決了以下幾類問題：

1.  **修復硬編碼的 URL**：
    - **問題**: 部分測試檔案（如 `ai_patrol.spec_v2.cjs`）中寫死了舊的伺服器位址。
    - **措施**: 已將其全部替換為相對路徑 (e.g., `/`)，以適應 Playwright 的動態 `baseURL`。

2.  **修復過時的 UI 導覽邏輯**：
    - **問題**: 大量的測試檔案仍然基於舊的單頁應用 (SPA) 邏輯，試圖點擊已不存在的「分頁按鈕」(`button[data-tab="..."]`) 來切換視圖。
    - **措施**: 已將這些點擊操作，全部重構為多頁應用 (MPA) 的直接頁面跳轉 (`page.goto('/some_page_v2.html')`)。

3.  **修正普遍的文本斷言錯誤**：
    - **問題**: 所有測試在等待 WebSocket 連線時，都在檢查一個過時的狀態文字「已連線」。
    - **措施**: 已根據目前前端的實際情況，將其全部更新為「準備就緒」。

4.  **修復掛起的單一測試**：
    - **問題**: 透過逐一排查，發現 `e2e.spec_v2.js` 中的一個測試案例會因為等待一個在模擬模式下永遠不會出現的「轉錄中...」狀態而導致測試掛起。
    - **措施**: 已修改該測試，使其斷言邏輯與模擬後端的實際行為（直接完成任務）保持一致。

5.  **引入狀態清理機制**：
    - **問題**: 懷疑測試之間存在狀態污染（一個測試的資料影響了下一個測試）。
    - **措施**: 已為**所有**測試檔案的 `beforeEach` 區塊中，都加入了 `page.request.post('/api/debug/clear_tasks')` 的呼叫，確保每個測試都在乾淨的資料庫環境下開始。

## 3. 當前瓶頸：頑固的「全局超時」問題

在完成了上述所有修復後，問題依舊存在。這指向了一個更深層次、更棘手的問題。

### 3.1. 問題的詭異之處 (Heisenbug)

- 當執行**完整測試套件** (`npx playwright test ...`) 時，流程會在約 400 秒後超時。
- 但是，當我**逐一單獨執行任何一個測試檔案**時，它們都能在幾秒鐘內快速完成（無論是通過還是失敗），**沒有任何一個檔案會單獨導致掛起**。

### 3.2. 核心原因推測：後端資源洩漏

這種「合則掛，分則不掛」的現象，強烈暗示問題的根源**不在於任何單一的測試檔案或前端邏輯**，而在於**後端伺服器在承受壓力時的穩定性**。

**我的核心假設是：資源洩漏 (Resource Leak)。**

當 Playwright 快速、高併發地執行大量測試時，後端 `api_server_v2.py` 會被頻繁請求。如果在處理請求的過程中，存在沒有被正確關閉的**檔案控制代碼 (File Handle)**、**資料庫連線**、或**子程序 (Subprocess)**，這些「洩漏」的資源會隨著時間不斷累積。當累積到作業系統的某個臨界點時，就可能導致整個伺服器沒有回應（無法接受新的請求或處理現有請求），從而讓後續的測試全部卡住，最終導致 Playwright 的全局超時。

## 4. 給下一位助理的偵錯建議

我已經窮盡了透過修改「測試端」來解決問題的方法。我強烈建議下一步的重點應該轉向**審查和加固「後端伺服器」**。

### 4.1. 最高優先級：為測試植入「心跳日誌」

這是我們討論過的、最有效的策略。與其被動地等待 400 秒的超時，不如主動地找出掛起的確切位置。

- **做法**：
  1.  選定一個最可疑的、最複雜的測試檔案（例如 `e2e_comprehensive_ui.spec_v2.js`）。
  2.  在該測試的每一個關鍵步驟之間，都插入 `console.log` 心跳。例如：
      ```javascript
      console.log("[Heartbeat] Step 1: Navigating to page...");
      await page.goto("/some_page");
      console.log("[Heartbeat] Step 2: Filling out form...");
      await page.locator("#my-input").fill("some value");
      console.log("[Heartbeat] Step 3: Clicking submit...");
      await page.locator("#submit-btn").click();
      console.log("[Heartbeat] Step 4: Waiting for result...");
      await expect(page.locator("#result")).toBeVisible();
      console.log("[Heartbeat] Test finished.");
      ```
- **目的**：再次執行完整測試套件。這次，當它再次掛起時，我們只需要查看終端輸出的**最後一條心跳日誌**，就能立刻知道程式是卡在哪一個具體的步驟上。這將會把問題的範圍從「整個測試套件」縮小到「某一個具體的非同步操作」。

### 4.2. 第二優先級：審查後端資源管理

在透過心跳日誌定位到具體的掛起點後（例如，發現總是在呼叫某個 API 後掛起），下一步就是審查後端 `api_server_v2.py` 中對應的端點。

- **檢查重點**：
  - **檔案操作**：是否有 `open()` 了檔案但從未 `close()`？（應該使用 `with open(...)` 結構來確保自動關閉）。
  - **子程序呼叫**：是否有 `subprocess.Popen` 或 `subprocess.run` 沒有正確處理 `stdout`/`stderr` 的管道，導致緩衝區被填滿而死鎖？
  - **資料庫連線**：雖然在模擬模式下是直接呼叫模組，但在真實模式下，是否有連線沒有被正確釋放？（需要檢查 `db/client_v2.py`）。
  - **非同步操作**：是否有 `async` 函式中存在沒有被 `await` 的非同步呼叫，導致了未被追蹤的背景任務？

祝你好運。這個問題非常棘手，但只要能定位到掛起的確切位置，就一定能解決。
