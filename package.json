{
  "name": "/app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "snapshot": "bun install && mkdir -p logs && python3 -m pip install -r requirements/server.txt -r requirements/gemini.txt -r requirements/transcriber.txt -r requirements/youtube.txt && npx playwright install chromium && bun scripts/snapshot.js",
    "test:e2e": "cross-env API_MODE=mock GOOGLE_API_KEY='DUMMY_KEY' npx playwright test src/tests/e2e_real_youtube_test.spec.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/bun": "latest",
    "jsdom": "^26.1.0",
    "playwright": "^1.45.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "cross-env": "^10.0.0",
    "wav": "^1.0.2"
  }
}
