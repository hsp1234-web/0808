# 前端架構重構 (v2) - 執行計畫與狀態

本文件旨在追蹤 v2 前端架構重構的進度、已完成的工作、以及後續的計畫。

## 總體目標
將前端從單頁應用 (SPA) 重構為多頁應用 (MPA)，並確保新架構的穩定性、可維護性與可測試性。

---

## 第一階段：偵錯與環境修復 (已完成)

此階段的目標是解決 v2 架構無法啟動 E2E 測試的根本問題。

### 已完成的修復工作 (Completed Tasks):

1.  **後端啟動流程修復**:
    *   **成果**: 後端伺服器現在可以在 `mock` 模式下為 Playwright 穩定、快速地啟動。

2.  **E2E 測試框架修復**:
    *   **成果**: Playwright 現在可以成功啟動後端伺服器、找到並執行測試案例。

---

## 第二階段：功能驗證與完善 (進行中)

此階段的目標是確保所有核心功能在新架構下都能通過自動化測試，並完成剩餘的開發任務。

### 已完成的工作 (2025-09-03)

1.  **解決模擬任務流程問題**:
    *   **問題**: E2E 測試因等待任務完成而超時。
    *   **解決方案**: 採納了**選項 A**，增強了 `api_server_v2.py` 的模擬能力，使其在 `mock` 模式下收到任務後，能立即將任務標記為「已完成」並透過 WebSocket 廣播更新。

2.  **E2E 測試的初步修復與診斷**:
    *   **修復前端 JS 執行錯誤**：發現並修復了 v2 HTML 頁面中因共享同一個大型 JS 腳本，導致在某些頁面上因找不到特定元素而使腳本執行中斷的嚴重問題。已透過為所有事件監聽器增加空值檢查來解決。
    *   **修復測試檔案命名規則**：發現並修復了 `playwright.config.v2.js` 未包含 `*.spec_v2.js` 等 v2 測試檔案的問題，使得所有相關測試都能被納入執行。
    *   **修復環境變數問題**：發現並修復了因測試執行器環境未設定 `API_MODE=mock`，導致部分測試檔案載入失敗的問題。
    *   **跳過不相關的測試**：識別並跳過了多個與 v2 `mock` 模式無關的測試（例如，需要真實 API 金鑰的測試、測試 v1 舊版 UI 的測試）。

### 當前卡點 (Current Blocker):

*   **大規模 E2E 測試失敗**: 在解決了上述基礎設施問題後，我們首次得以執行完整的 v2 測試套件。結果顯示，儘管伺服器已能啟動且核心模擬功能正常，但仍有大量的測試案例 (28/38) 失敗。
*   **失敗的根本原因**:
    1.  **硬編碼的 URL**: 絕大多數失敗的測試，都在其程式碼中寫死了舊的、固定的伺服器位址 (如 `http://localhost:8001`)，導致無法連線到由 Playwright 動態啟動的測試伺服器。
    2.  **過時的 UI 定位器 (Locators)**: 部分測試仍在嘗試尋找 v1 單頁應用 (SPA) 中的 UI 元素 (如 `#tabs`)，這些元素在 v2 的多頁應用 (MPA) 架構中已不存在。
    3.  **UI/UX 斷言不一致**: 部分測試對於 UI 上顯示的文字（如狀態標籤）的預期，與目前前端的實際顯示不符。

### 後續計畫 (Next Steps):

1.  **✅ (進行中) 系統性修復 E2E 測試**:
    *   **第一優先**：批量修復所有測試檔案中的**硬編碼 URL** 問題，將其改為使用相對路徑，以適應 Playwright 的動態 `baseURL`。
    *   **第二優先**：修正所有因 UI 架構變更而**過時的元素定位器**和**不一致的文字斷言**。
    *   **第三優先**：解決剩餘的功能性 Bug，直到整個測試套件（已跳過的除外）達到 100% 通過。

2.  **重構前端 CSS**: 根據使用者「內嵌所有 CSS」的決定，整理並確保所有五個 MPA 頁面的樣式都已正確內嵌在各自的 HTML 檔案中。

3.  **最終審查與交付**:
    *   執行程式碼格式化與靜態檢查。
    *   請求程式碼審查 (`request_code_review`)。
    *   更新 `Log.md` 和 `AGENTS.md`。
    *   提交最終的、完整的、通過所有測試的程式碼。
