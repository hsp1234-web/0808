# 前端架構重構 (v2) - 執行計畫與狀態

本文件旨在追蹤 v2 前端架構重構的進度、已完成的工作、以及後續的計畫。

## 總體目標

將前端從單頁應用 (SPA) 重構為多頁應用 (MPA)，並確保新架構的穩定性、可維護性與可測試性。

---

## 第一階段：偵錯與環境修復 (已完成)

此階段的目標是解決 v2 架構無法啟動 E2E 測試的根本問題。

### 已完成的修復工作 (Completed Tasks):

1.  **後端啟動流程修復**:
    - **成果**: 後端伺服器現在可以在 `mock` 模式下為 Playwright 穩定、快速地啟動。

2.  **E2E 測試框架修復**:
    - **成果**: Playwright 現在可以成功啟動後端伺服器、找到並執行測試案例。

---

## 第二階段：功能驗證與完善 (進行中)

此階段的目標是確保所有核心功能在新架構下都能通過自動化測試，並完成剩餘的開發任務。

### 已完成的工作 (2025-09-03)

1.  **解決模擬任務流程問題**:
    - **問題**: E2E 測試因等待任務完成而超時。
    - **解決方案**: 採納了**選項 A**，增強了 `api_server_v2.py` 的模擬能力，使其在 `mock` 模式下收到任務後，能立即將任務標記為「已完成」並透過 WebSocket 廣播更新。

2.  **E2E 測試的初步修復與診斷**:
    - **修復前端 JS 執行錯誤**：發現並修復了 v2 HTML 頁面中因共享同一個大型 JS 腳本，導致在某些頁面上因找不到特定元素而使腳本執行中斷的嚴重問題。已透過為所有事件監聽器增加空值檢查來解決。
    - **修復測試檔案命名規則**：發現並修復了 `playwright.config.v2.js` 未包含 `*.spec_v2.js` 等 v2 測試檔案的問題，使得所有相關測試都能被納入執行。
    - **修復環境變數問題**：發現並修復了因測試執行器環境未設定 `API_MODE=mock`，導致部分測試檔案載入失敗的問題。
    - **跳過不相關的測試**：識別並跳過了多個與 v2 `mock` 模式無關的測試（例如，需要真實 API 金鑰的測試、測試 v1 舊版 UI 的測試）。

### 當前卡點 (Current Blocker):

- **頑固的 E2E 全局超時**: 儘管已對測試套件進行了多輪、系統性的修復（包括 URL、導覽邏輯、文本斷言、狀態清理等），但在執行完整測試套件時，依然會發生全局性的超時。
- **問題的複雜性**:
  1.  **無法單獨重現**: 單獨執行**任何一個**測試檔案都能在短時間內完成（無論通過或失敗），問題只在執行完整測試套件時出現。
  2.  **核心原因推測**: 這種現象強烈暗示問題的根源並非單一的前端或後端 Bug，而是**後端伺服器在承受高併發壓力下的資源洩漏或競爭條件**。

### 後續計畫 (Next Steps):

**首要目標：解決全局超時問題，讓測試套件能夠完整地跑完一次。**

1.  **🔴 (新) 偵錯後端伺服器穩定性 (最高優先級)**:
    - **方法**: 根據 `HANDOVER_PLAN.md` 和 `AGENTS.md` 中記錄的偵錯策略，對後端伺服器進行壓力測試與審查。
    - **步驟一：植入心跳日誌**：挑選一個複雜的測試案例，在其中加入詳細的 `console.log` 心跳，以在下一次完整測試執行時，精確定位到發生掛起的程式碼位置。
    - **步驟二：審查後端程式碼**：一旦定位到可疑的 API 端點，立即審查 `api_server_v2.py` 中對應的程式碼，重點檢查檔案處理、子程序呼叫、非同步操作等是否存在資源洩漏的可能。

2.  **🟡 (暫緩) 修復已知的前端與測試錯誤**:
    - 在全局超時問題解決之前，暫時擱置對已知錯誤的修復。
    - 一旦測試套件能完整跑完，`Log.md` 和 `HANDOVER_PLAN.md` 中記錄的、那些在單獨執行時會失敗的測試（如 API 金鑰驗證、任務列表未更新等），將成為下一個要解決的目標。

3.  **🟡 (暫緩) 最終審查與交付**:
    - 在所有測試（包括超時問題和功能性錯誤）都解決後，再進行程式碼格式化、最終審查和提交。
