# 前端架構重構進度報告與後續計畫 (v2)

**文件版本**: 1.0
**更新日期**: 2025-09-03

## 第一部分：已完成進度 - 前端 MPA 架構重構

### 1.1. 核心目標
將原有的單頁應用 (SPA) 架構，重構為一個更穩定、更易於維護、頁面間完全隔離的多頁應用 (MPA) 架構。

### 1.2. 執行摘要
我們成功地在一個隔離的 `src_v2` 開發軌道中，完成了對前端架構的全面重構。所有前端頁面現在都是完全獨立的 HTML 檔案，各自內嵌了所需的 CSS 與 JavaScript，移除了所有外部依賴和資源共享，達成了使用者追求的極致穩定性目標。後端 API 也已完成相應的路由調整，以支援新的 MPA 結構。

### 1.3. 詳細變更項目

#### 1.3.1. 建立隔離的開發環境
- **建立 `src_v2`**: 完整複製 `src` 目錄至 `src_v2`，所有重構工作均在此目錄下進行，確保不影響 `src` 的穩定性。
- **建立快照備份**: 在執行任何操作前，已將原始 `src` 目錄的完整內容備份至 `archive/app_snapshot_[時間戳記]`，作為安全還原點。

#### 1.3.2. 前端 MPA 重構 (位於 `src_v2/static/`)
- **頁面拆分**: 將原有的 `mp3.html` 拆分為五個獨立的、功能專一的頁面：
    - `index.html`: 主儀表板，顯示進行中與已完成任務。
    - `transcribe.html`: 本機檔案轉錄功能頁。
    - `downloader.html`: 通用媒體下載器頁面。
    - `youtube_report.html`: YouTube 轉報告功能頁。
    - `prompts.html`: AI 提示詞管理頁面。
- **資源內嵌**: 遵照指示，將所有 CSS 樣式和 JavaScript 腳本直接內嵌到每一個 HTML 檔案的 `<style>` 和 `<script>` 標籤中。
    - **成果**: 實現了頁面的完全自給自足，消除了因共享資源或外部 CDN 造成的潛在不穩定性。
- **導覽更新**: 將原先用於在 SPA 內部切換視圖的「頁籤按鈕」，修改為在不同 HTML 頁面之間跳轉的標準超連結 (`<a href="...">`)。

#### 1.3.3. 後端路由適配 (位於 `src_v2/api/api_server.py`)
- **修改根路由**: 將 FastAPI 的根路由 (`/`) 從指向舊的 `mp3.html`，修改為指向新的主頁 `index.html`。
- **新增通用路由**: 建立了一個新的通用路由 `/{page_name}.html`，使其能夠正確地提供 `static` 目錄下所有頂層的 HTML 頁面，以支援前端的頁面跳轉。

---

## 第二部分：已完成進度 - `src_v2` 全面重新命名

**狀態：** ✅ **已完成** (程式碼重構完成，但 E2E 測試因環境問題受阻)

### 2.1. 核心目標
遵照您的指示，為 `src_v2` 目錄下的**所有**檔案（不論類型）加上 `_v2` 後綴，以徹底消除與 `src` 目錄在追蹤問題時的潛在混淆，提升長期可維護性。

### 2.2. 詳細執行步驟

1.  **檔案系統重新命名**: ✅ **已完成**
    - **範圍**: `src_v2` 目錄下的所有檔案。
    - **操作**: 透過自動化腳本，遍歷 `src_v2` 下所有子目錄，將每一個檔案重新命名為 `*_v2` 版本。
    - **例外**: 目錄名稱 (如 `api`, `static`) 保持不變。

2.  **更新 Python 程式碼引用**: ✅ **已完成**
    - **更新 Imports**: 已透過腳本修正所有 `_v2.py` 檔案的內部 `import` 語句。
    - **更新檔案路徑**: 已更新所有寫死的檔案路徑字串，包括 `subprocess` 呼叫中的路徑。

3.  **更新後端路由**: ✅ **已完成**
    - **目標檔案**: `api_server_v2.py`。
    - **操作**: 已修改 FastAPI 路由以提供 `*_v2.html` 檔案，並更新了所有指向 `src/` 的內部路徑變數。

4.  **更新前端 HTML 檔案**: ✅ **已完成**
    - **目標檔案**: `src_v2/static/` 下的所有 `*_v2.html` 檔案。
    - **操作**: 已修正所有頁面間的導覽連結，將 `href` 更新為 `page_v2.html`。

5.  **驗證與測試**: ⚠️ **已執行但受阻**
    - **靜態檢查**: 已完成，所有關鍵檔案的變更都已通過手動抽查確認。
    - **動態測試 (E2E)**: 嘗試執行，但因伺服器啟動超時而失敗。根本原因因無法取得偵錯日誌而未能定位。此步驟是「軌道切換」的前提條件，目前被視為一個已知的阻礙。
任務 3.1 (建立 v2 測試環境):

行動: 修改 Playwright 設定 (playwright.config.js) 或建立一個新的設定檔，專門用於啟動和測試 src_v2 中的伺服器。這可能需要為 v2 伺服器指定一個不同的埠號（例如 42650），以避免與穩定版衝突。

任務 3.2 (為新頁面編寫獨立測試):

行動: 為 src_v2 中的每一個新頁面 (index.html, downloader.html 等) 編寫一個全新的、獨立的 E2E 測試腳本。

允收標準: 所有針對 src_v2 的新 E2E 測試必須 100% 穩定通過。

階段四：軌道切換 (The Promotion)
前提: 階段三的所有 v2 驗證測試已成功通過。

任務 4.1 (安全封存舊軌道):

行動: 執行 python scripts/time.py 獲取時間戳記。

行動: 將原始的 src 目錄重新命名為 src_legacy_[時間戳記]，並移動到 archive/ 資料夾中。

任務 4.2 (正式化新軌道):

行動: 將 src_v2 目錄重新命名為 src。

階段五：最終迴歸測試
任務 5.1 (執行基準線測試):

行動: 在新的 src 目錄（即原 src_v2）上，重新運行階段零的基準 E2E 測試套件。

允收標準: 原有的基準測試必須再次 100% 通過。這證明了我們的重構在功能上完全向下相容，且未引入任何衰退 (Regression)。

至此，重構正式完成。
