# 前端架構重構 (v2) - 執行計畫與狀態

本文件旨在追蹤 v2 前端架構重構的進度、已完成的工作、以及後續的計畫。

## 總體目標
將前端從單頁應用 (SPA) 重構為多頁應用 (MPA)，並確保新架構的穩定性、可維護性與可測試性。

---

## 第一階段：偵錯與環境修復 (已完成)

此階段的目標是解決 v2 架構無法啟動 E2E 測試的根本問題。

### 已完成的修復工作 (Completed Tasks):

1.  **後端啟動流程修復**:
    *   **問題**: 後端服務在測試環境下因多程序通訊死鎖、設定錯誤等原因，無法啟動，導致 E2E 測試超時。
    *   **解決方案**:
        *   **實作簡化版 `mock` 模式**: 重寫了 `orchestrator_v2.py` 和 `api_server_v2.py`，在 `mock` 模式下，只啟動 API 伺服器，並讓其直接存取資料庫，繞過所有複雜的程序間通訊。
        *   **修正 Python 環境**: 解決了 `pyenv` 版本不一致、`PYTHONPATH` 未設定、Python 套件結構不正確 (`__init__.py` 缺失) 等一系列環境問題。
    *   **成果**: 後端伺服器現在可以在 `mock` 模式下為 Playwright 穩定、快速地啟動。

2.  **E2E 測試框架修復**:
    *   **問題**: Playwright 無法正確找到並執行所有 v2 相關的測試案例。
    *   **解決方案**:
        *   **修正設定檔路徑**: 修正了 `playwright.config.v2.js` 中指向不存在的 `src_v2` 目錄的錯誤。
        *   **修正測試語法**: 將所有使用 CommonJS (`require`) 的 `.spec.js` 測試檔案，全部更新為專案所設定的 ES Module (`import`) 語法。
        *   **修正測試檔案命名**: 發現 `_v2.js` 的檔名格式會導致 Playwright 無法探索，將其重命名後解決了問題。
        *   **修正測試案例邏輯**: 修正了多個測試案例中因 UI 改版而過時的元素定位器 (locators) 和錯誤的 URL。
    *   **成果**: Playwright 現在可以成功啟動後端伺服器、找到並執行測試案例。

---

## 第二階段：功能驗證與完善 (進行中)

此階段的目標是確保所有核心功能在新架構下都能通過自動化測試，並完成剩餘的開發任務。

### 當前卡點 (Current Blocker):

*   **模擬任務流程中斷**: 在執行一個需要完整任務生命週期（提交 -> 處理 -> 完成）的 E2E 測試時，測試因等待任務完成而超時。
    *   **原因**: 我們簡化後的 `mock` 模式只啟動了 API 伺服器，但沒有一個模擬的 `Worker` 去處理任務並將其標記為「完成」。

### 後續計畫 (Next Steps):

1.  **✅ (待決策) 解決模擬任務流程問題**: 等待使用者從以下三個選項中選擇一個解決方案：
    *   **選項 A (推薦)**: 增強 API 伺服器的模擬能力，讓它在收到任務後立即將其標記為完成。
    *   **選項 B**: 嘗試啟動一個模擬的 `Worker` 程序。
    *   **選項 C**: 暫時跳過此類複雜的測試。

2.  **修復所有 E2E 測試**: 在上述問題解決後，全面運行所有 E2E 測試，並修復所有可見的 Bug，直到整個測試套件 100% 通過。

3.  **重構前端 CSS**: 根據使用者「內嵌所有 CSS」的決定，整理並確保所有五個 MPA 頁面的樣式都已正確內嵌在各自的 HTML 檔案中。

4.  **最終審查與交付**:
    *   執行程式碼格式化與靜態檢查。
    *   請求程式碼審查 (`request_code_review`)。
    *   更新 `Log.md` 和 `AGENTS.md`。
    *   提交最終的、完整的、通過所有測試的程式碼。
